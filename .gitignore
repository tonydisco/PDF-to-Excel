# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# PyInstaller build output
build/
dist/
dmg_root/
tesseract/
*.manifest
*.dmg

# OS
.DS_Store
Thumbs.db

# Dữ liệu nhạy cảm — KHÔNG đưa báo cáo/ô tài chính lên repo
*.pdf
*.xlsx
*.xls

# Giữ lại gói OCR tiếng Việt (cần cho ứng dụng)
!tessdata/vie.traineddata

# Demo cục bộ (đường dẫn máy cá nhân)
chay_thu_CLI.command
Demo_output/

# Benchmark: dữ liệu thật & đáp án (số tài chính nhạy cảm) — KHÔNG commit
bench/pdfs/
bench/truth/*.json
# Bảng benchmark thô có tên đầy đủ file client — tái tạo được, không commit
bench/results-*.md
# Thư mục dữ liệu mẫu của người dùng (PDF + danh sách công ty) — KHÔNG commit
sample data/
# Cache OCR/benchmark cục bộ
work/
bench/out/

# Đóng gói sidecar (PyInstaller) — binary nặng, KHÔNG commit
dist_sidecar/
build_sidecar/
desktop/src-tauri/binaries/
